#include <string>
#include <iostream>

int main()
{
    std::string pattern;
    std::getline(std::cin, pattern);

    std::string sentence;
    std::getline(std::cin, sentence);
	for (char c : pattern) {
			if (c == 'D')
			
	}
    std::cout << sentence << "\n";
}
